extends layout
block content
	script.
		console.log('#{message}');
	.container
		.errorWrap
			h1 Error
			.error
				if (status === 500)
					h2 Sorry, looks like something went wrong.
				if (status === 404)
					h2 Woops! Looks like you got a bit lost!
				.errorImage
					img(src="/images/error_#{status}.png", alt='error image')
				a(href="/")
					button#retry(type="button") Go back
				//-h2= message